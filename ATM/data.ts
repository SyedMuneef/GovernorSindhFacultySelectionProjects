//Defining types and structure for data to Generate Randomly

interface IuserData {
  id: number;
  name: string;
  pin: number;
  accoutNumber: number;
  blance: number;
}

export default IuserData;
